# `b0923d4` 旧版功能审计与现版本重构报告

审计日期：2026-08-04  
唯一旧版基准：`b0923d471abbf85f0bf88fbb635cefbbbb041e37`  
现版本审计起点：`6537aeca1c46a79e6f780a194de22dd94e00973f`

## 1. 审计口径

本轮把旧提交导出为独立只读快照，逐项核对旧版 83 个受版本控制文件、14 个 HTML 入口、31 个 JavaScript 文件、样式、SQL、数据脚本和测试脚本，并与当前 React/Vite/TypeScript 版本的路由、页面、数据服务、状态管理、图片链路、安全边界和视觉层级对照。

详细功能定义与长期维护矩阵见：

- `docs/baseline-b0923d4-functional-spec.md`
- `docs/baseline-b0923d4-maintenance-matrix.md`
- `docs/current-version-gap-audit.md`
- `docs/legacy-baseline-audit.md`

## 2. 功能对标结论

| 范围 | 对标结果 | 现版本证据 |
| --- | --- | --- |
| F01 邀请码门禁 | 等价实现 | `AuthProvider`、`AccessGate`、90 天滑动/365 天绝对期限、返回目标地址与清权流程保留 |
| F02 导览首页 | 已补齐 | 统计、主入口、次级入口、彩蛋和“历史上的今天”保留；历史入口移入页头动作区 |
| F03 导航与生命周期 | 等价实现 | React Router 懒加载、滚动恢复、全屏偏好、移动导航与统一页头 |
| F04 普通记录 | 已补齐并优化 | 全筛选、URL 同步、隐藏序列、附件、正文引用和精确定位保留；首屏改为最小数据请求 |
| F05 书面记录 | 已补齐并优化 | 页面、留言、补充、翻页、图片查看与引用定位保留；仅在书面视图加载 |
| F06 正文标记语言 | 等价实现 | 类型化 AST、安全 React 节点渲染、19 类标记、递归/深度/失败降级测试 |
| F07 插图和大图 | 等价实现 | 私有签名、尺寸门、预览、缩放、拖动、重试上限与路径隔离 |
| F08–F10 人物/人物详情/名言 | 已补齐 | 分组独立排序、人物模式切换、来源解析与唯一定位均保留 |
| F11 全站搜索 | 等价实现 | 分组索引、权重、摘要、高亮、URL 与防抖保留 |
| F12–F13 普通/管理员答题 | 已补齐 | 三层等权抽样、三类题型、稳定填空、隐藏题解锁和逐字答案保留 |
| F14 时间线 | 已补齐 | 全局、年度、月度、每日层级与条数/字数指标、来源下钻保留 |
| F15 补充资料 | 已补齐并扩容 | URL 直达、目录/正文双独立滚动；工作区、正文宽度、字号和行距扩大 |
| F16 蹭饭图 | 等价实现 | 元数据和签名 URL 单一职责、完整 contain、大图查看与边界重试 |
| F17 背景主题 | 已补齐 | 选择、预览、取色、持久化、预解码交叉淡入保留；遮罩和内容表面透明度统一 |
| F18 制作组与致谢 | 已补齐 | 动态标题、章节、感谢、附件与标记正文保留 |
| F19 缓存和加载协调 | 已补齐并优化 | memory/session/IDB、并发合并、授权纪元与部分可用保留；取消跨页面强制档案预热 |
| F20–F21 Supabase/RLS/管理脚本 | 未改动安全边界 | 普通/管理员隔离、短签名、CLI 校验/重试/显式 prune 约束保持 |
| F22 404/部署边界 | 等价实现 | 未知路由、公有门禁页、SPA 回退和部署安全头契约保持 |

结论：22 组旧版能力均已在当前架构中存在或采用等价实现。本轮没有删除旧版业务能力，也没有修改数据库表、RLS、Storage policy、邀请码、service-role 或私有资源。

## 3. 本轮发现并修复的代码级问题

1. 全局图片元数据预加载器会在每个受保护路由强制加载记录、人物和派生名言，放大首屏网络与 CPU 开销。
2. 记录列表依赖全局 archive 聚合资源，必须等待与当前页面无关的人物、名言数据。
3. 普通记录视图也会请求书面页、留言和补充资料。
4. 每次搜索输入都会重复解析全部正文标记，并重复构造年月日选项。
5. 筛选条件被写入列表容器 `key`，导致每次筛选整棵记录列表卸载重建。
6. 记录卡片和引用回调缺少稳定边界，父页面更新会扩大重渲染范围。
7. 各页面在正文区重复显示大标题，顶部栏只有品牌名，造成留白和信息层级重复。
8. 首页“历史上的今天”固定在左上角，可能压住导航层。
9. 背景遮罩和内容表面透明度不统一，用户背景被过度遮挡。
10. 资料页虽已有双滚动区，但重复页头和偏保守的宽度/内距压缩了有效阅读面积。
11. 页头动作最初采用桌面/移动双实例隐藏方案；代码复核后改为 `matchMedia` 单实例响应式挂载，避免重复 ID、隐藏控件和重复副作用。

## 4. 性能改进

- 普通记录首屏从“记录 + 人物 + 派生名言 + 书面页 + 留言 + 补充”的聚合等待，收敛为单独 `loadRecords()`。
- 书面页、留言和补充仅在 `view=written` 时并发加载。
- 全局预加载器不再主动调用 `archive.ensure()`；只有已被业务页面加载的档案数据才在浏览器空闲期补元数据。
- 普通记录页只在空闲期预热最新记录中的最多 16 个插图路径，并限制并发为 3。
- 正文搜索文本使用记录对象级 `WeakMap` 缓存；日期选项使用 `useMemo`，不随搜索词重算。
- 移除筛选容器动态 `key`；`RecordCard` 使用 `memo`，引用跳转使用稳定 `useCallback` 与最新状态引用。
- 构建仍按路由拆包；生产构建转换 2,817 个模块，记录页产物约 15.47 kB（gzip 6.34 kB）。

这些是确定性的请求依赖和渲染范围削减。由于本轮没有可用的普通用户访问令牌，未把开发环境网络波动下的单次计时包装成性能百分比。

## 5. UI、布局和可读性

- 新增共享页头上下文：顶栏统一显示 `编日史 > 当前页面`，页面业务标题不再在正文区重复。
- 页面动作通过 React Portal 进入顶栏；窄屏自动回到正文动作区，且任一断点只挂载一份控件。
- 内容工作区最大宽度扩大到 96rem；固定视口页面减少外层 padding。
- 资料页目录/正文比例改为约 22%/78%，正文最大宽度扩至 68rem，并保留两个独立 `ScrollArea`。
- 普通正文采用约 16–17.2px 响应式字号和 1.9 行高；资料正文约 17–18.4px 和 1.95 行高；人物业务表格和辅助信息同步增大。
- 内容表面改为统一半透明渐变，顶栏使用独立半透明层；图片背景暗化减弱并增加轻量暗角，默认纸张背景补充细纹理。
- 所有基础 UI 继续组合既有 shadcn 组件；`frontend/src/components/ui` 无改动。

## 6. 修改文件

业务组件与页面：

- `frontend/src/components/layout/page-header.tsx`（新增）
- `frontend/src/components/layout/app-shell.tsx`
- `frontend/src/components/layout/background-root.tsx`
- `frontend/src/components/archive/page-heading.tsx`
- `frontend/src/components/archive/record-card.tsx`
- `frontend/src/components/archive/record-filters.tsx`
- `frontend/src/features/archive/image-metadata-preloader.tsx`
- `frontend/src/pages/home-page.tsx`
- `frontend/src/pages/materials-page.tsx`
- `frontend/src/pages/people-page.tsx`
- `frontend/src/pages/records-page.tsx`
- `frontend/src/styles/tailwind.css`

测试契约：

- `scripts/test-illustration-preload.mjs`
- `scripts/test-record-view.mjs`
- `scripts/test-static-site.mjs`

## 7. 验证结果

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：11 组安全、门禁、缓存、图片、正文、记录、答题、搜索和静态契约全部通过。
- `npm run build`：通过；沙箱内原生 Windows 绑定受限，获准在沙箱外执行后成功。
- 公共门禁浏览器检查：受保护的 `/records` 正确重定向到 `/auth`；桌面与 390×844 视口均无横向溢出，主操作高度 44px。
- `git diff -- frontend/src/components/ui`：为空。

## 8. 旧版自身的已知问题

旧提交的单项核心测试（安全图片、加载、门禁、缓存、插图、正文、记录视图、答题、搜索）可分别通过，但旧快照的完整 `npm test` 不能原样全绿：

- `private-assets` 不在该提交的受控文件中，相关测试缺少输入。
- 旧版提交内 `vendor/supabase-js-2.45.0.js` 与静态测试记录的 2.45.0 SRI 不一致。

现版本使用 npm/Vite 依赖构建，不沿用这条旧 vendor 校验链路；这两项属于基准提交自身的可复现缺口，不应被误记为本轮回归。

## 9. 未完成项、风险与建议

唯一未闭环项是“普通最终用户 token → RPC 复验 → RLS → 私有 Storage 签名 → 真实数据逐页交互”的 A 类浏览器验收。本轮没有读取或消耗邀请码文件，也没有把私有令牌、签名 URL 或资源写入代码/日志。类型、测试、生产构建、公共门禁和代码级数据链路均已验证，但它们不能替代这条最终授权链路。

建议在发布前使用一枚有效普通邀请码执行一次只读验收，重点覆盖：记录列表/书面视图切换、资料双滚动区、背景切换、人物/名言来源定位、答题、时间线和蹭饭图大图。若该验收通过，本轮不存在已知功能欠缺；若失败，应优先判断为环境/RLS/数据契约问题，再回到对应 F01、F05、F07、F15、F16、F20 项定位。

## 10. 2026-08-05 统计页专项复核与收口

### 10.1 旧版结构与现版偏差

旧版 `timeline.html` 的阅读顺序是“总览指标 → 整体记录人占比/全档案月趋势 → 年度记录人占比/年份选择 → 月度记录人占比/月度选择 → 月度摘要 → 每日柱形图 → 七列日历 → 记录人图例与洞察”。专项复核发现，重构初版虽保留了数据能力，但存在四项结构偏差：

1. 全档案月趋势给图表写入动态 `minWidth`，外层再启用 `overflow-x-auto`，因此页面主动制造了横向滚动区域。
2. 年份控件同样使用水平滚动容器，月份分布与月份选择被拆开，破坏旧版“左图右选项”的连续阅读关系。
3. 年度区域额外插入一张柱形图，使总览、年份、月份三个层级不再清晰。
4. 指标、年份或月份变化时，页面根节点使用动态 `key` 整体卸载重建；饼图同时叠加 Recharts 浮动 `Tooltip` 与 550ms 动画，首次悬停时内容测量和位置过渡叠加，造成提示框或周边视觉元素跳动。

源码中没有“上三角形—横线—下三角形”对应的业务组件、图标导入或事件处理。它不是一个遗漏功能，而是强制滚动容器在浏览器/操作系统中生成的滚动条附属视觉。删除制造溢出的宽度与容器后，该图案随根因一并消失。

### 10.2 专项修复

- 统计页恢复旧版层级和左右配对布局：整体占比/全档案趋势、年度占比/年份选择、月度占比/月度选择；删除无基准依据的年度附加柱形图。
- 全档案和每日柱形图改为固定高度、`width: 100%`、`min-width: 0` 的响应式容器，不再给数据点数量换算像素宽度；年份和月份改为会自动换行的语义化按钮网格。
- 日历恢复桌面七列、窄屏四列；年份和月份卡头在手机端上下排列，操作按钮不再挤压标题。
- 饼图回归旧版稳定交互：保留常驻图例与扇区高亮，移除无基准依据的浮动提示框。柱形图仍保留精确值提示，但关闭 Tooltip 位置动画并固定内容宽度；饼图入场动画从 550ms 降至 320ms。
- 删除统计页、人物页、人物列表、名言页、搜索页用于筛选状态的根节点动态 `key`，避免每次切换把整个结果树卸载重建。
- 统计聚合新增记录日期、字符数、人物/名言提及、参与者和记录人 `WeakMap` 缓存；一次性构造年/月索引并复用于指标卡、图表、日历和洞察，减少重复正文解析和重复筛选。
- 统计页单独获得 90rem 可用宽度；其他普通页面保持原有上限。没有改动只读目录 `frontend/src/components/ui`。
- 页面标题继续统一进入顶栏；人物详情按需求例外处理：顶栏固定显示“人物详情”，具体姓名留在正文 `h1`。
- “不要外传”只在首页/引导页显示；记录页和蹭饭图不再重复。首页使用可见但克制的隐私提示，蹭饭图仅保留“班级私有资源”标签。
- 背景表面、正文透明度、字号、行高和资料阅读宽度沿用本轮统一方案，统计页新增内容也使用同一套卡片层级和排版尺度。

当前 [Recharts Tooltip 文档](https://recharts.github.io/en-US/api/Tooltip/)说明提示层支持位置动画，[ResponsiveContainer 文档](https://recharts.github.io/en-US/api/ResponsiveContainer/)要求由可收缩容器提供尺寸；本次实现据此关闭柱形图 Tooltip 的位置动画，并移除固定最小宽度。[shadcn Chart 文档](https://ui.shadcn.com/docs/components/chart)推荐由外层提供明确高度并让图表容器占满宽度，本次柱形图按该组合方式落地。

### 10.3 新增回归契约与验证

- `scripts/test-static-site.mjs` 新增统计页无 `overflow-x-auto`、无动态 `minWidth`、无根节点选择态 `key`、饼图无浮动 Tooltip、柱形图 Tooltip 无位置动画、旧版年/月选择结构和七列日历等契约。
- 新增人物详情顶栏/正文标题分离、“不要外传”唯一位置、首页今日历史动作单实例、筛选结果树不整体重挂载等契约。
- `npm run typecheck`：通过。
- `npm run lint`：通过，53 个前端文件无修复项。
- `npm test`：通过，11 组测试全部绿色；静态应用契约覆盖 60 个 CLI 生成的 shadcn Base UI 组件。
- `npm run build`：通过，Vite 8.2.0 共转换 2,817 个模块；统计页保持路由级懒加载，产物 376.09 kB，gzip 111.05 kB，不进入其他页面的首屏路由包。
- 浏览器公共门禁验收：1280×720、1024×768、390×844 三个视口均满足文档 `scrollWidth === clientWidth`，没有可见横向溢出节点；输入框与主按钮高度均为 44px，Google Sans Flex/Geist 字体栈生效，控制台无警告或错误。
- `git diff --check`：通过；`git diff -- frontend/src/components/ui`：为空。

### 10.4 剩余真实环境边界

当前内置浏览器没有普通用户授权状态，Chrome/Edge 扩展浏览器也不可用，因此受保护的 `/timeline` 会按设计跳转到 `/auth`。本轮没有读取邀请码、检查浏览器凭据存储、伪造 session 或加入开发绕过。统计页的数据计算、交互结构、响应式契约、类型、测试和生产构建均已闭环；仍需在发布前使用一枚有效普通邀请码完成一次真实数据只读验收，重点观察首次图例悬停、年份/月切换、每日图表与日历、记录来源跳转以及四个目标视口是否仍无横向滚动。

构建还显示现有 `GoogleSansFlex` 可变 TTF 为 3,997.14 kB，并使用 `font-display: swap` 避免阻塞文字显示。当前工作区没有可验证的 WOFF2 无损转换/字形覆盖检查工具，因此本轮没有用不完整子集冒险替换它；后续应在字体许可允许的前提下，用可复现的字体流水线生成 WOFF2，并对中文回退、拉丁字形、变量轴和浏览器兼容性做视觉回归。
